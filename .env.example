# Copy to .env and chmod 600. NEVER commit the real file.
#   cp .env.example .env && chmod 600 .env
#
# One line per channel; the name must match `stream_key_env` in its YAML.
CHANNEL01_STREAM_KEY=xxxx-xxxx-xxxx-xxxx-xxxx
CHANNEL02_STREAM_KEY=
CHANNEL03_STREAM_KEY=
CHANNEL04_STREAM_KEY=
CHANNEL05_STREAM_KEY=
